train == test:
* temp4 -- convolution misspecification (tempcv)
* temp5 -- contaminated model (epsilon = 0.2 change with uniform (-dist_bd, dist_bd)

train != test:
* temp6
train: contaminate model with epsilon = 0.2, change with uniform (-dist_bd, dist_bd)
test: true parametric

* temp7
train: change with another set of beta distributions (shift_prop = 5).
alpha_i <- alpha_i + (alpha_i - alpha_lb)/shift_prop if 2 * alpha_i > alpha_lb + alpha_ub
alpha_i <- alpha_i -(alpha_ub - alpha_i)/shift_prop else


